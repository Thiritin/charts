apiVersion: v1
kind: Service
metadata:
  name: {{ include "laravel-helm-deployment.fullname" . }}
  labels:
    {{- include "laravel-helm-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laravel-helm-deployment.selectorLabels" . | nindent 4 }}
